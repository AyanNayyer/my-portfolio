import React from "react";

const Projects = () => (
  <section id="projects">
    <h2>My Projects</h2>
    <div className="project-grid">
      {/* Add your project items here */}
    </div>
  </section>
);

export default Projects;
